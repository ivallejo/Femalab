@model Femalab.Model.Entities.Attention

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <!-- Custom fonts for this template-->
    @Styles.Render("~/Content/adminfont")
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">

    <!-- Custom styles for this template-->
    @Styles.Render("~/Content/admincss")
    @Styles.Render("~/Content/admintablescss")
    @Styles.Render("~/Content/toas")


</head>
<body>

    <br />
    <br />
    <br />
    <br />
    <div class="container">
        <div class="bd-example">

            <dl class="row">
                <dt class="col-sm-9"><strong class="mr-4">RECIBO N°  </strong>  @Model.Id.ToString().PadLeft(6, '0') </dt>
                <dd class="col-sm-3"><strong>S/. @Model.AttentionDetails.Sum(x => x.Product.Price)</strong></dd>
            </dl>
            <br>
            <dl class="row">

                <dt class="col-sm-3">Recibí del Sr. (a) :</dt>
                <dd class="col-sm-9">@Model.Patient.LastName,  @Model.Patient.FirstName </dd>

                <dt class="col-sm-3">La suma de :</dt>
                <dd class="col-sm-9">S/. @Model.AttentionDetails.Sum(x => x.Product.Price) </dd>

                <dt class="col-sm-3">Por :</dt>
                <dd class="col-sm-9"> @String.Join(", ", Model.AttentionDetails.Select(p => p.Product.Description).ToArray())</dd>

                <dt class="col-sm-8"></dt>
                <dd class="col-sm-4">Lima, 21 de Abril del 2019</dd>

            </dl>

        </div>
    </div>

    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <div class="container">
        <div class="bd-example">

            <dl class="row">
                <dt class="col-sm-9"><strong class="mr-4">RECIBO N°  </strong>  @Model.Id.ToString().PadLeft(6, '0') </dt>
                <dd class="col-sm-3"><strong>S/. @Model.AttentionDetails.Sum(x => x.Product.Price)</strong></dd>
            </dl>
            <br>
            <dl class="row">

                <dt class="col-sm-3">Recibí del Sr. (a) :</dt>
                <dd class="col-sm-9">@Model.Patient.LastName,  @Model.Patient.FirstName </dd>

                <dt class="col-sm-3">La suma de :</dt>
                <dd class="col-sm-9">S/. @Model.AttentionDetails.Sum(x => x.Product.Price) </dd>

                <dt class="col-sm-3">Por :</dt>
                <dd class="col-sm-9"> @String.Join(", ", Model.AttentionDetails.Select(p => p.Product.Description).ToArray())</dd>

                <dt class="col-sm-8"></dt>
                <dd class="col-sm-4">Lima, 21 de Abril del 2019</dd>

            </dl>

        </div>
    </div>

    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <div class="container">
        <div class="bd-example">

            <dl class="row">
                <dt class="col-sm-9"><strong class="mr-4">RECIBO N°  </strong>  @Model.Id.ToString().PadLeft(6, '0') </dt>
                <dd class="col-sm-3"><strong>S/. @Model.AttentionDetails.Sum(x => x.Product.Price)</strong></dd>
            </dl>
            <br>
            <dl class="row">

                <dt class="col-sm-3">Recibí del Sr. (a) :</dt>
                <dd class="col-sm-9">@Model.Patient.LastName,  @Model.Patient.FirstName </dd>

                <dt class="col-sm-3">La suma de :</dt>
                <dd class="col-sm-9">S/. @Model.AttentionDetails.Sum(x => x.Product.Price) </dd>

                <dt class="col-sm-3">Por :</dt>
                <dd class="col-sm-9"> @String.Join(", ", Model.AttentionDetails.Select(p => p.Product.Description).ToArray())</dd>

                <dt class="col-sm-8"></dt>
                <dd class="col-sm-4">Lima, 21 de Abril del 2019</dd>

            </dl>

        </div>
    </div>


    @Scripts.Render("~/bundles/adminjs")
    @Scripts.Render("~/bundles/adminpuglin")
    @Scripts.Render("~/bundles/admintablesjs")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/toas")
    @Scripts.Render("~/bundles/attention")
    <script src="~/Scripts/attention/invoice.js"></script>

    @*<script type="text/javascript">
        
        win = window.open();
        win.document.write(document.documentElement.innerHTML);

        var document_focus = false; // var we use to monitor document focused status.
        // Now our event handlers.
        $(document).ready(function () { win.window.print(); document_focus = true; });
        setInterval(function () { if (document_focus === true) { win.window.close(); } }, 300);
        
    </script>*@

</body>
</html>
